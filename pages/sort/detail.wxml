
<view class="images">
  <image class="image" mode="{{item.height>item.width?'aspectFill':'aspectFit'}}" wx:for="{{images}}" wx:key="id" src="{{item.url}}" bindtap="bindTapImage" data-index="{{index}}" />
  <view style="clear: both;"></view>
</view>
<view class="lastest">全部加载完成</view>